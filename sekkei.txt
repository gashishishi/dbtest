データベースを読み込み、内容を表示するプログラムを作る。

Books DPOクラスを継承する。
DPOクラス + データベースbooksテーブルを操作する関数を追加した感じにする。
変数
title
isbn
price
publish
author

必要な関数
xss対策
db open(try catch)static function 戻り値 :PDO PDOオブジェクトを返す
データベースの内容を表示する(list.phpの内容)
データベースに追加する(add.phpの内容)
  → プレースホルダーとかそういうのの関数も必要っぽい

入力内容のバリデーションを行う関数

<?php
class Ppppo{
    public static $avc = 456;
    const PPP = 123;
}

echo Ppppo::$avc;
        // ↑スコープ演算子 クラス名::変数or関数名


list.html → edit() →(getでidを渡す)  edit.php idバリデーション、問題なければ対象idデータをDBから取得
取得した各データをvalueに設定したformを作る。method="post" → update.php → update()

edit()
  